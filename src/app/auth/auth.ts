export class Auth {
    constructor(
        public id: Number,
        public phoneno: Number,
        public password: String,
        public name: String,
        public isadmin: Number,
        public pincode: Number,
        public area: String
    ) {
    }
}